---
interface Props {
    id?: string;
    class?: string;
}

const { id, class: className } = Astro.props;
---

<section
    id={id}
    class={`flex max-w-7xl min-h-screen py-20 sm:py-24 md:py-32 lg:py-36 mx-auto ${className}`}
>
    <slot />
</section>
