---
interface Props {
    id?: string;
    class?: string;
}

const { id, class: className } = Astro.props;
---

<section id={id} class={`flex max-w-7xl h-screen py-36 mx-auto ${className}`}>
    <slot />
</section>
